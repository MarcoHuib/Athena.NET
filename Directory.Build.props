<Project>
  <PropertyGroup>
    <!-- Keep NuGet's resolved dependency graph under source control per project. -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
  </PropertyGroup>
</Project>
